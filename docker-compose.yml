version: '3.8'

services:
  backend:
    build: ./mern-demo/server
    container_name: mern-backend
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb+srv://GiaBao:1234%40qwer@cluster0.w0bbevh.mongodb.net/cloud_lab?retryWrites=true&w=majority
      - PORT=5000

  frontend:
    build: ./client
    container_name: mern-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend